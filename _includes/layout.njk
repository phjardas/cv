<!DOCTYPE html>
<html lang="{{ language }}">
  <head>
    {% metagen
    title = metadata.title + " · " + metadata.subtitle,
    desc = description,
    locale = language,
    type = "website",
    css = ["/styles.css"],
    js = ["/assets/main.js"]
 %}
    {% for lang in metadata.languages %}
      <link rel="alternate" hreflang="{{ lang }}" href="{{ metadata.url }}/{{ lang }}/"/>
    {% endfor %}
  </head>
  <body>
    <div class="page" id="page">
      {{ content | safe }}
    </div>
  </body>
</html>