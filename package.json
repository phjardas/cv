{
  "name": "jardas-cv",
  "version": "3.0.0",
  "private": true,
  "homepage": "https://cv.jardas.de/",
  "scripts": {
    "build": "npm run compile && eleventy",
    "start": "npm run compile && eleventy --serve --incremental",
    "compile": "npx esbuild --bundle --minify --outdir=assets src/main.js"
  },
  "volta": {
    "node": "21.6.2"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1",
    "eleventy-plugin-metagen": "^1.8.3",
    "eleventy-sass": "^2.2.4",
    "esbuild": "^0.20.1",
    "html-minifier": "^4.0.0",
    "normalize-scss": "^8.0.0",
    "nunjucks": "^3.2.4",
    "prettier": "^3.2.5"
  }
}
